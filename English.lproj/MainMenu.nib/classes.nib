{
    IBClasses = (
        {
            ACTIONS = {changeAi = id; changeLevel = id; newGame = id; undo = id; }; 
            CLASS = ABController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                aiButton = NSButton; 
                board = BoardView; 
                level = NSTextField; 
                levelStepper = NSStepper; 
                turn = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = BoardView; LANGUAGE = ObjC; SUPERCLASS = NSMatrix; }, 
        {
            ACTIONS = {changeAi = id; changeLevel = id; changeSize = id; newGame = id; undo = id; }; 
            CLASS = Desdemona; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                aiButton = NSButton; 
                black = NSTextField; 
                board = BoardView; 
                level = NSTextField; 
                levelStepper = NSStepper; 
                size = NSTextField; 
                sizeStepper = NSStepper; 
                turn = NSTextField; 
                white = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = DesdemonaBoard; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {checkForUpdates = id; }; 
            CLASS = SUUpdater; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}